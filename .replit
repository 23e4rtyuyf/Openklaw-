run = "pip install -r requirements.txt -q && cd frontend && npm install --legacy-peer-deps -q && npm run build && cd .. && python main.py"
entrypoint = "main.py"

[nix]
channel = "stable-23_11"

[[ports]]
localPort = 8080
externalPort = 80
